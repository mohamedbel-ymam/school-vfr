APP_NAME="Takwa Établissement"
APP_ENV=production
APP_KEY=base64:JhVbJnADc37xEx6llk/S05VGuqL7iIbBCYJDtGRIyQI=   # keep your existing key
APP_DEBUG=false
APP_URL=https://api.takwaetablissement.com
FRONTEND_URL=https://takwaetablissement.com

APP_LOCALE=fr
APP_FALLBACK_LOCALE=fr
APP_FAKER_LOCALE=fr_FR
APP_TIMEZONE=Africa/Casablanca

LOG_CHANNEL=stack
LOG_LEVEL=info

# ---- DB (use the DB you create on Forge) ----
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=school_management          # or 'forge' or whatever you create
DB_USERNAME=forge                      # or the user you create
DB_PASSWORD=GYdbwravh290zk8ZjGRD                # set this in Forge DB tab

# ---- Sessions / Sanctum (critical) ----
SESSION_DRIVER=cookie
SESSION_DOMAIN=.takwaetablissement.com   # leading dot = valid for all subdomains
SESSION_LIFETIME=120
SESSION_SECURE_COOKIE=true               # HTTPS only cookies in prod
SESSION_SAME_SITE=lax                    # ok for subdomain SPA + API

# List domains WITHOUT scheme/port; include your SPA origins.
SANCTUM_STATEFUL_DOMAINS=takwaetablissement.com,www.takwaetablissement.com

# ---- Queues / Files / Cache ----
QUEUE_CONNECTION=sync
FILESYSTEM_DISK=public
CACHE_STORE=file

# ---- Redis (optional; if not using, leave as-is or comment) ----
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ---- Mail (use your provider: Mailgun, Brevo, etc.) ----
MAIL_MAILER=smtp
MAIL_HOST=smtp.yourprovider.tld
MAIL_PORT=587
MAIL_USERNAME=your_smtp_user
MAIL_PASSWORD=your_smtp_pass
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=no-reply@takwaetablissement.com
MAIL_FROM_NAME="${APP_NAME}"

# ---- Misc ----
PHP_CLI_SERVER_WORKERS=4
VITE_APP_NAME="${APP_NAME}"
